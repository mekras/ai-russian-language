---
name: ru-lang
description: >
  Используй, когда агент пишет, редактирует, улучшает или проверяет русский
  текст: собственные промежуточные и итоговые ответы пользователю, пояснения,
  объявления, формулировки, замены запрещённых слов и словосочетаний, работу
  с терминами, брендами, URL и техническими элементами. Используй как базовый
  слой для русского текста, включая случаи, где более узкий навык добавляет
  свои правила.
---

# ru-lang

Применяй русский язык аккуратно во всех русскоязычных текстах: сохраняй
смысл, проверяемость и естественный порядок слов.

## Быстрый стоп-лист для отчётов агента

В собственных русских статусах, промежуточных сообщениях и итоговых отчётах
перед отправкой замени:

- `JSON-файл`, `JSON-файлы`, `JSON‑файл`, `JSON‑файлы` → `файл JSON`,
  `файлы JSON`;
- `APM-доставляемая инструкция`, `APM-зависимости` →
  `инструкция, доставляемая через APM`, `зависимости APM`;
- `Claude-инструкция`, `Claude-инструкции` → `инструкция Claude`,
  `инструкции Claude`;
- `Claude-результат`, `single-file ветка` → `результат Claude`,
  `ветка single-file` или `ветка одиночного режима`;
- `` `ветка single-file` ``, `` `ветки single-file` `` →
  `ветка single-file`, `ветки single-file`;
- `` `ветке single-file` `` → `в ветке single-file`;
- `DNS-серверы`, `DNS-пакеты`, `DNS-настройки`, `AAAA-ответ`,
  `` `AAAA`-ответ ``, `A-запрос`, `` `A`-запрос `` → `серверы DNS`,
  `пакеты DNS`, `настройки DNS`, `ответ AAAA`, `запрос A`;
- `конфиг`, `конфиге`, `конфига` → `конфигурация`, `настройки`,
  `файл конфигурации`;
- `lockfile-хеш` → `хеш файла блокировки`;
- `итоговый diff`, `Итоговый diff` → `итоговый набор изменений`,
  `итоговые изменения`;
- `staged-файл`, `staged-файлы` → `файл в индексе Git`,
  `файлы в индексе Git`, `подготовленный к коммиту файл`;
- `skill-каталог`, `skill-каталоги` → `каталог навыка`,
  `каталоги навыков`;
- `trigger-проверка`, `trigger-проверки`, `trigger evals` →
  `проверка срабатывания`, `проверки срабатывания`;
- `result-сценарий`, `result-сценарии` → `сценарий результата`,
  `сценарии результата`;
- `кейс`, `кейсы` → `случай`, `пример`, `сценарий`;
- `валиден`, `валидна`, `валидно`, `валидны`, `корректен`, `корректна`,
  `корректно`, `корректны` → `прошёл проверку`, `прошли проверку`,
  `без ошибок`, `соответствует требованиям`, `правильно`.
- `корректный`, `корректная`, `корректное`, `корректные`, `некорректный`,
  `некорректная`, `некорректное`, `некорректные` → `правильный`,
  `неправильный`, `ошибочный`, `точный`, `неточный` по смыслу.

## Когда применять

Используй навык, когда итогом должен стать русский текст для человека:

- ответ пользователю;
- правка или улучшение русской формулировки;
- подбор русской замены англицизму;
- работа с терминами, брендами, URL и другими техническими элементами в
  русском тексте.

## Когда не применять

- итогом должен стать не русский текст;
- задача сводится к коду, команде, SQL-запросу или переименованию
  идентификаторов без русского текста;
- нужны проектные правила разработки поверх базового языкового слоя:
  в таком случае используй `ru-dev` поверх `ru-lang`.

## Обязательный порядок

1. Если доступен `ru-text`, используй его как базовый редакторский слой.
   `ru-lang` не дублирует методику `ru-text`, а задаёт прикладные и
   технические правила русского текста.
2. Если правила расходятся, применяй их в таком порядке: явное требование
   пользователя, терминология проекта или предметной области, смысл исходного
   материала, затем рекомендации `ru-text` и `ru-lang`.
3. Для любого русского текста сначала проверь
   [базовые языковые правила](./references/base-language.md).
4. Если текст содержит технические термины, гибридные формы, бренды, режимы
   доступа, URL или другие специальные элементы, дополнительно проверь
   [правила технического русского текста](./references/technical-russian.md).
5. Для выбора конкретной замены используй
   [словарь замен](./assets/term-replacements.md) и
   [примеры гибридных форм](./assets/hybrid-examples.md).
6. Перед отправкой собственного русского статуса, отчёта или промежуточного
   сообщения быстро перечитай черновик как обычный русский текст: обратные
   кавычки, английская форма слова или привычный рабочий жаргон сами по себе не
   делают слово идентификатором. Если это не имя файла, команда, параметр,
   поле, API, путь, класс, метод или цитата, переведи или перестрой форму.
   Не бери русское словосочетание целиком в обратные кавычки, чтобы сохранить
   английскую часть: пиши `в ветке single-file`; не пиши так, будто вся фраза
   `ветка single-file` является идентификатором.
   Обратные кавычки в примерах этого навыка служат разметкой инструкции; не
   копируй их в итоговый русский текст вокруг обычных русских слов и фраз.
   Отдельно проверь корни и формы из служебных отчётов: `корректн`,
   `валидн`, `кейс`, `skill`, `trigger`, `result`, `eval`, `staged`, а также
   связки латиницы, сетевой аббревиатуры или имени продукта с русским словом
   через любой дефисный знак: `APM-доставляемая`, `Claude-инструкции`,
   `lockfile-хеш`, `DNS-серверов`, `AAAA-ответа`, `A-запрос`,
   `Claude-результат`, `APM-зависимостей`, а также английские режимы и
   названия вариантов перед русским существительным: `single-file ветка`,
   `read-only режим`. В диагностических отчётах отдельно проверяй
   англоязычные рабочие термины вроде `stub`, `upstream`, `cache`, `legacy` и
   `degraded mode`: если это не идентификатор и не точная цитата, переведи или
   поясни их по роли в системе.
   Не отправляй русский текст, если эти формы остались в обычной фразе:
   перепиши их до ответа.

## Ограничения

- Навык не заменяет правила проекта, редакционную политику, юридические
  требования или отраслевые ограничения.
- Не русифицируй идентификаторы, имена API, команды, параметры, названия
  пакетов, классов, методов и файлов.
- Не изменяй цитаты, URL, команды и другие технические идентификаторы без
  явной задачи редактуры.
- Если `ru-text` недоступен, продолжай работу по правилам `ru-lang` и упоминай
  это только для строгой редакторской проверки, установки навыков или
  диагностики.
